aw shit, here we go again.

    sudo pacman -Rsn alacritty cachyos-zsh-config vim micro cachyos-micro-settings meld btop btrfs-assistant cachyos-hello system-config-printer cachyos-kernel-manager scx-manager
    sudo pacman -S file-roller liquidctl discord steam gnome-calculator xed
    sudo modprobe k10temp

    git clone https://github.com/lojithos/thimblefans.git
    cd thimblefans
    ./install.sh
    sudo mv fan_speed_control /bin/

    # Run startup applications and set fan_speed_control as a startup application, and run it while you're there
